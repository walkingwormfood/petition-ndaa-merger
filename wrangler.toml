name = "petition-ndaa-merger"
compatibility_date = "2026-07-01"
pages_build_output_dir = "."

[vars]
SITE_URL = "https://petition-ndaa-merger.pages.dev"
MAIL_FROM = "petition@example.invalid"   # set to a real verified sender before production
TEST_MODE = "1"                          # remove (or set "0") for production — test mode returns the confirm link directly

[[d1_databases]]
binding = "DB"
database_name = "petition-signatures"
database_id = "b478b5ca-bc3a-4724-b5f4-d802c9229199"
